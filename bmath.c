#include "bmath.h"
#include "tokens.h"
//#include "main.h"
#include "string.h"

int read_int(char *to_read) {

    return atoi(to_read);
}

float read_single(char *to_read) {

    return atof(to_read);
}
